# Placeholder for Open Graph Image
# The final Open Graph image should be 1200x630 pixels and named `og-image.jpg`.
# Place it in the root `public` folder so it resolves to https://wasamchaudhry.com/og-image.jpg.
